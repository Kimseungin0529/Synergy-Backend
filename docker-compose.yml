version: '3.8'

services:
  app:
    image: wjd4204/github-action-synergy
    ports:
      - "8080:8080"
    environment:
      - TZ=Asia/Seoul
      - ./synergy.env
    depends_on:
      - redis

  redis:
    image: "redis:latest"
    ports:
      - "6379:6379"