version: '3.8'

services:
  app:
    image: wjd4204/github-actions-synergy:latest
    ports:
      - "8080:8080"
    environment:
      - TZ=Asia/Seoul
    env_file:
      - ./synergy.env
    depends_on:
      - redis

  redis:
    image: "redis:latest"
    ports:
      - "6379:6379"